import * as React from "react";

interface IPersonDisplayProps {
  name: string;
  age: number;
  height: number;
  isMinor: boolean;
}

export const PersonDisplay = (props: IPersonDisplayProps) => {
  return <div />;
};
